import React from 'react';
import { Box, Typography } from '@mui/material';

const RelatedDevice = ({ relatedDevicesData }) => {

  return (
    <Box>
      <Typography variant="h6">Related Devices:</Typography>
    </Box>
  );
};

export default RelatedDevice;
